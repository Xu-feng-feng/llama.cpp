[project]
name = "lfm25-gpu-report-tools"
version = "0.1.0"
description = "Normalization and report dependencies for the LFM2.5 GPU benchmark"
requires-python = ">=3.11,<3.14"
dependencies = [
    "huggingface-hub>=0.34,<2",
    "matplotlib>=3.10,<4",
    "pandas>=2.3,<3",
    "python-docx>=1.2,<2",
]

[tool.uv]
package = false
