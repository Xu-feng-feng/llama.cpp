› 为什么采用 BF16 mmap写入只需要数GiB峰值显存，先解释上述词汇的含义


• 核心原因：BF16 mmap 转换并没有把整个 354 GB 模型同时放进内存，更没有放进 CUDA 显存。它采用“磁盘映射 + 分块处理”，任意时刻只处理一个分片
  或一个小块。

  ## 词汇含义

   词汇           含义
  ━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   BF16           16 位浮点格式，每个参数占 2 字节；FP32 每个参数占 4 字节
  ─────────────  ────────────────────────────────────────────────────────────────────
   mmap           将磁盘文件映射到进程虚拟地址空间，需要哪一页才由操作系统读取哪一页
  ─────────────  ────────────────────────────────────────────────────────────────────
   RAM / 内存     CPU 使用的主机内存
  ─────────────  ────────────────────────────────────────────────────────────────────
   VRAM / 显存    CUDA GPU 使用的模型、KV cache 和计算缓冲区
  ─────────────  ────────────────────────────────────────────────────────────────────
   RSS            进程当前真正占用的物理内存
  ─────────────  ────────────────────────────────────────────────────────────────────
   VIRT           进程映射的虚拟地址范围，不代表真正占用了同等物理内存
  ─────────────  ────────────────────────────────────────────────────────────────────
   分块处理       每次只转换固定数量的数据，完成后释放临时结果
  ─────────────  ────────────────────────────────────────────────────────────────────
   峰值内存       整个过程中某一时刻最大的物理内存占用

  需要特别区分：convert_hf_to_gguf.py 主要使用 CPU，通常没有 CUDA 权重分配。因此这里的“数 GiB 峰值显存”更准确地说是“数 GiB 主机内存峰
  值”。DGX Spark 使用统一内存，物理内存池由 CPU/GPU共享，所以监控结果有时容易混淆。

  ## mmap 为什么不需要装下整个文件

  PLE 张量的尺寸是：

  320,001,536 行 x 160 个元素

  如果使用 FP32：

  320001536 x 160 x 4
  = 204,800,983,040 字节
  约 190.7 GiB

  转换成 BF16 后：

  320001536 x 160 x 2
  = 102,400,491,520 字节
  约 95.4 GiB

  使用 mmap 时，程序虽然可以访问完整的 190.7 GiB 文件，但它只是建立虚拟地址映射。操作系统不会立刻把 190.7 GiB 全部读入 RAM。

  可以类比成一本很厚的书：

  - 普通读取：把整本书复印到桌面上。
  - mmap：书仍放在书架上，需要哪一页就取哪一页。

  所以进程可能显示接近 200 GiB 的 VIRT，但实际 RSS 只有几 GiB。

  ## 分块 BF16 转换占用多少内存

  代码每次处理：

  1 << 16

  即 65,536 行。

  每块 FP32 输入：

  65536 x 160 x 4 = 40 MiB

  每块 BF16 输出：

  65536 x 160 x 2 = 20 MiB

  一次循环大致是：

  磁盘 mmap 的 40 MiB FP32
          ↓
  转换临时缓冲
          ↓
  20 MiB BF16 写回磁盘
          ↓
  释放临时缓冲

  专门的完整分块测试中，这部分峰值 RSS 约为 467 MiB，而不是 190 GiB。

  ## 为什么完整转换仍可能达到数 GiB

  峰值主要来自单个模型分片的反量化，而不是整个 PLE 表。

  FP8 PLE 共有 128 个分片。程序每次只处理一个：

  1. 从 safetensors 加载一个 FP8 分片。
  2. 应用共享 weight_scale。
  3. 临时展开成 FP32。
  4. 写入 mmap 文件。
  5. 释放这个分片。
  6. 再处理下一个分片。

  一个较大的 FP8 分片展开成 FP32 后可能达到 1-3 GiB；再加上源数据、PyTorch 临时张量、量化输出和文件页缓存，完整转换的峰值就可能是数 GiB。

  关键是峰值近似：

  最大单个分片 + 转换临时缓冲

  而不是：

  全部 128 个分片之和

  ## 原地压缩为什么安全

  FP32 每行占：

  160 x 4 = 640 字节

  BF16 每行只占：

  160 x 2 = 320 字节

  输出只有输入的一半，因此可以从文件开头向前写：

  读取当前 FP32 块
  -> 转换到临时 BF16 块
  -> 将 BF16 写到文件前半部分

  BF16 写入位置始终位于尚未读取的 FP32 数据之前，不会覆盖后面的未处理数据。全部完成后，把临时文件截断到原来的一半。

  ## mmap 的代价

  mmap 并没有消除数据，只是把主要压力从 RAM 转移到了磁盘：

  - 需要约 190.7 GiB FP32 临时磁盘空间。
  - 转换后临时文件缩小到约 95.4 GiB。
  - 最终还要写出约 354 GB 的 GGUF。
  - 速度受到 NVMe 和操作系统页缓存影响。

  因此，这种方案实现的是：

  少量 RAM + 大量磁盘空间 + 顺序磁盘读写

  而普通的 torch.cat 或整张量 BF16 转换则需要约 190 GiB 甚至更多的物理内存，在当前 121 GiB DGX Spark 上会被 OOM kill。
